module.exports = function (M) {
const {add, P, H1, H2, R, table, FIG, NUM, f, f0, pct, CITE} = M;
const FA = NUM.factorial || {},
      RC = NUM.recovery || null, LF = NUM.leverage_forms, BY = (NUM.factorial||{}).by || {};
const CN = NUM.concentration || {}, CD = NUM.conc_diff || null, CT = NUM.conc_targets || null;
const FX2 = (NUM.fixedn||{}).sd_mean;
const TW = NUM.threeway || null, TN = NUM.threeway_nested && NUM.threeway_nested.colon ? NUM.threeway_nested : null;
const ivn  = z => "[" + f(z.ci80[0],2) + " to " + f(z.ci80[1],2) + "]";
const ivn1 = z => "[" + f(z.ci[0],1) + " to " + f(z.ci[1],1) + "]";
const iv2 = z => "[" + f(z.ci80[0],2) + " to " + f(z.ci80[1],2) + "]";
const TG = NUM.targets || null, SZ = NUM.sim_sizes || null;
const fk = x => Number(x).toLocaleString("en-US").replace(/,/g, "\u2009");
const NS = NUM.nested || {}, FO = NUM.frontier_opt || null;
const PR = NUM.primary || {};
const TS = NUM.trainsize || null, POP = NUM.pop_rule || null,
      CF = NUM.confound || null, CL = NUM.confound_limits || null, NCol = NUM.noncollapse || null, PL = NUM.plate || null;
const has = x => x !== undefined && x !== null;

add(H1("Results"));

/* ---------- 1 ---------- */
add(H2("The two promises are different kinds of claim"));
add(P("Table 1 gives the out-of-sample comparison against the original protocol for both cohorts. The eligible count behaved as the monotonicity argument requires: across 500 colon splits the minimum difference was exactly zero, never negative. The rule removed at least one criterion in " + pct(PR.colon.removed_any) + " of splits, and the removals bound on the scoring half in " + pct(PR.colon.removed_binding) + ", so the figure usually reported for this promise describes how often a binding criterion is removed rather than whether the right ones were."));
if (PR.colon && NS.colon) {
  const C = PR.colon, R = PR.rott, NC = NS.colon, NR = NS.rott;
  /* Reviewer round 8, major points 1 and 4. These are not confidence intervals and
     are no longer written as though they were. The central 80% of the outer
     bootstrap distribution leads, because at these outer counts its endpoints rest
     on about the tenth replicate from each end while the 2.5 and 97.5 points rest
     on about the third; the wider span follows to one decimal, which is all it can
     support. Nothing in the text turns on where either endpoint falls. */
  const iv  = z => "[" + f(z.ci80[0],2) + " to " + f(z.ci80[1],2) + "]";
  const iv1 = z => "[" + f(z.ci[0],1) + " to " + f(z.ci[1],1) + "]";
  add(P("Whether the rule does more than remove criteria needs a comparison at equal inclusiveness, which our first attempt did not provide: it matched random relaxations on the NUMBER of criteria removed, not on how many patients they admitted. We therefore score all 512 subsets on the held-out half of each split, compute the proportion within a split, and average splits with equal weight. The comparator set excludes the rule's own subset, so the proportion is over strictly other protocols. Comparisons are strict inequalities, so a comparator that ties the rule exactly on the hazard ratio or the restricted mean counts against it rather than for it; the frequency of exact ties is reported in Supplementary Table S1d. Point estimates come from the full " + C.R + " and " + R.R + " splits. Beside each we give a PATIENT-RESAMPLED RANGE: the cohort is bootstrapped over patient identifiers, the whole split procedure is rerun inside each resample, and we report the central 80% of the resulting values, with the wider 2.5 to 97.5 per cent span in square brackets after it to one decimal. These are not confidence intervals. Each outer replicate is built from fewer inner splits than the point estimate uses, so the spread carries inner Monte Carlo variance a population range would not; the correction the supplement reports for that is a linear shrinkage that matches a variance and is not known to recover quantiles; and we have not evaluated coverage against a known population. The Methods set this out in full. We use the ranges to say how far each statistic moves when the patients are resampled, and for nothing else: no conclusion below turns on whether a range covers one half."));
  add(P("Matched to within ten per cent on eligible count — a mean of " + f0(C["nm0.1"]) + " comparators per split in colon and " + f0(R["nm0.1"]) + " in Rotterdam, sitting symmetrically around the rule's own count (mean relative difference " + f(C["sk0.1"],3) + " and " + f(R["sk0.1"],3) + ") — the rule reaches a lower log hazard ratio than a matched subset in " + pct(C["hr0.1"]) + " of colon comparisons, central 80% patient-resampled range " + iv(NC["hr0.1"]) + " and wider span " + iv1(NC["hr0.1"]) + ", and " + pct(R["hr0.1"]) + " in Rotterdam, " + iv(NR["hr0.1"]) + " and " + iv1(NR["hr0.1"]) + ". Each central range is about a third of the unit interval wide and straddles one half, which is the substance of what these cohorts can say about the matched comparison. All four tolerances and both estimands are given with their ranges in Supplementary Table S3. The estimates are stable across matching tolerances from 2 to 15 per cent, moving between " + pct(Math.min(C["hr0.02"],C["hr0.05"],C["hr0.1"],C["hr0.15"])) + " and " + pct(Math.max(C["hr0.02"],C["hr0.05"],C["hr0.1"],C["hr0.15"])) + " in colon."));
  add(P("On the restricted mean difference the two cohorts disagree. In colon the rule is below one half at every tolerance (" + pct(C["rm0.1"]) + " at ten per cent, range " + iv(NC["rm0.1"]) + "); in Rotterdam it is above (" + pct(R["rm0.1"]) + ", " + iv(NR["rm0.1"]) + "). Each of these central ranges is between a quarter and three tenths of the unit interval wide. We do not read them as tests and make no claim either way about any population value: what these cohorts support is that the quantity is not pinned down at equal inclusiveness, on either estimand, in either cohort — and the disagreement between the two estimands in colon is itself worth noting, given that the paper shows hazard-ratio movement need not track absolute benefit."));
  add(P("The frontier comparison must be read with more care than we gave it. A subset dominates the rule if it admits at least as many patients at a log hazard ratio at least as low, judged on the held-out half; on that criterion the rule's choice was on the frontier in " + pct(C.front_hr) + " of colon splits and " + pct(R.front_hr) + " of Rotterdam splits, with a mean of " + f0(C.n_dom) + " and " + f0(R.n_dom) + " subsets dominating. Requiring a margin of " + f(C.margin,2) + " on the log hazard ratio — roughly a five per cent relative change, an investigator-chosen sensitivity margin rather than a threshold with any general clinical meaning, and reported because the bare inequality is sensitive to arbitrarily small differences — raises frontier membership to " + pct(C.front_hrM) + " and " + pct(R.front_hrM) + ". But this is an EMPIRICAL held-out frontier, taken as the best of 512 noisy estimates on the same data used to judge it, and it is optimistic."));
  if (FO) {
    add(P("Simulation sizes that optimism, at a bare inequality rather than with a margin. With the population known, a held-out half the size of a colon scoring set shows a median of " + f0(FO.dom_emp) + " apparently dominating subsets where the population has " + f0(FO.dom_pop) + ", and puts the rule off the frontier in " + pct(1-FO.front_emp) + " of replicates against " + pct(1-FO.front_pop) + " at the population. Of the subsets dominating on one held-out half, " + pct(FO.repro) + " still dominate on an independent half and only " + pct(FO.true_frac) + " genuinely dominate at the population. These numbers therefore measure the rule's distance from an optimistic oracle constructed on the same data, not from the true optimum; simulation shows the direction of the bias and its size in one generating mechanism, and does not convert the observed-cohort frontier into an unbiased one (Supplementary Table S3b)."));
  }
  if (NUM.threeway) {
    const TC = NUM.threeway.colon, TR = NUM.threeway.rott;
    add(P("The same question can be put to the cohorts themselves without a generating model, by splitting three ways: the rule is fitted on one third, the subsets that dominate it are identified on a second, and exactly those subsets — fixed before the third is touched — are re-evaluated on a third that has been used for nothing. Three quantities come out of this and they must be kept apart, because we conflated two of them in the previous version. The first is empirical and optimistic: on the selection third a median of " + f0(TC.dom_sel) + " subsets dominate the rule in colon and " + f0(TR.dom_sel) + " in Rotterdam, leaving it undominated in only " + pct(TC.front_sel) + " and " + pct(TR.front_sel) + " of splits."));
    add(P("The second is the only quantity here evaluated on patients that took no part in choosing the comparator, because the comparator set was fixed in advance. It is an internally validated estimate from a sample split, not a confirmatory result in an independent cohort: the same patients are reused across all splits, so it addresses selection optimism and not external validity. Of the subsets that dominate on the selection third, a median of " + f0(TC.n_survive) + " in colon and " + f0(TR.n_survive) + " in Rotterdam still dominate on the untouched third — a replication rate of " + pct(TC.repro) + " and " + pct(TR.repro) + (TN ? ", central 80% patient-resampled ranges " + ivn(TN.colon.repro) + " and " + ivn(TN.rott.repro) + " (wider spans " + ivn1(TN.colon.repro) + " and " + ivn1(TN.rott.repro) + ")" : "") + " — and the single lowest-hazard-ratio dominator carries over in " + pct(TC.best_holds) + " and " + pct(TR.best_holds) + " of splits" + (TN ? ", ranges " + ivn(TN.colon.best_holds) + " and " + ivn(TN.rott.best_holds) : "") + ". For that last quantity the estimated inner Monte Carlo variance is at least the whole spread across resamples, so subtracting one from the other leaves nothing to report: the patient-level component is not identifiable at the Monte Carlo precision we can afford. That is a statement about what we could measure, not a finding that patient sampling contributes no variability (Supplementary Table S3c). Requiring the " + f(PR.colon.margin,2) + " margin on the selection third leaves a median of " + f0(TC.dom_selM) + " and " + f0(TR.dom_selM) + " candidates and changes the replication rate to " + pct(TC.reproM) + " and " + pct(TR.reproM) + ". Taking the same view of frontier membership, the rule is left undominated by every pre-selected subset in " + pct(TC.front_conf) + " of colon splits and " + pct(TR.front_conf) + " of Rotterdam splits" + (TN ? ", ranges " + ivn(TN.colon.front_conf) + " and " + ivn(TN.rott.front_conf) + ", from an outer bootstrap over patients wrapped around the whole three-way procedure at " + TN.colon.B + " resamples by " + TN.colon.R + " splits, against the " + f0(TC.R) + " and " + f0(TR.R) + " splits behind the point estimates" : "") + ". These ranges carry the same caveats as the others in this paper. At " + (TN ? TN.colon.B : 100) + " outer resamples the wider span rests on about the third replicate from each end, which is why the central 80% leads and the span is printed to one decimal; Supplementary Table S3c gives the Monte Carlo error of both. They describe how far the statistics move under patient resampling, and no conclusion here turns on where an endpoint falls. This is the quantity we ask a reader to carry away, and the comparison that isolates selection optimism is against the selection third of this same design, not against the half-split figures reported earlier, which also use a larger evaluation set."));
    add(P("The third is descriptive only. Re-scanning all 512 subsets on the test third gives a median of " + f0(TC.dom_test) + " and " + f0(TR.dom_test) + " dominating subsets and leaves the rule undominated in " + pct(TC.front_test) + " and " + pct(TR.front_test) + " of splits. These counts are not the survivors of the second quantity (Supplementary Table S3c gives all three blocks separately) — they come from a fresh selection on the very data used to judge it — so this is a second empirical oracle rather than an out-of-sample evaluation, and it should be read only as showing that a similar number of apparent dominators exists in any third of these cohorts. Across all three views the rule is off the frontier in the great majority of splits; what the out-of-sample version changes is the number of subsets one should believe are genuinely better, not the qualitative finding. How far the rule sits from the true best attainable protocol, these cohorts cannot tell us."));
  }

add(table(
  ["Out-of-sample comparison", "colon (n = 619)", "Rotterdam (n = 2982)"],
  [["Endpoint; horizon; splits", C.time_var+", "+C.status_var+"; "+f0(C.tau)+" d; "+C.R,
                                 R.time_var+", "+R.status_var+"; "+f0(R.tau)+" d; "+R.R],
   ["Original protocol, out of sample", "n = "+f0(C.n_full)+", HR = "+f(C.hr_full,3)+", RMST = "+f(C.rm_full,1)+" days",
                                        "n = "+f0(R.n_full)+", HR = "+f(R.hr_full,3)+", RMST = "+f(R.rm_full,1)+" days"],
   ["Data-driven protocol, out of sample", "n = "+f0(C.n_rule)+", HR = "+f(C.hr_rule,3)+", RMST = "+f(C.rm_rule,1)+" days",
                                        "n = "+f0(R.n_rule)+", HR = "+f(R.hr_rule,3)+", RMST = "+f(R.rm_rule,1)+" days"],
   ["Eligible patients: difference; P(more)", "+"+f(C.d_n,1)+"; "+f(C.more,3), "+"+f(R.d_n,1)+"; "+f(R.more,3)],
   ["Hazard ratio: difference; P(lower)", (C.d_hr>0?"+":"")+f(C.d_hr,4)+"; "+f(C.lower,3), (R.d_hr>0?"+":"")+f(R.d_hr,4)+"; "+f(R.lower,3)],
   ["RMST: difference; P(greater)", (C.d_rm>0?"+":"")+f(C.d_rm,1)+" days; "+f(C.greater,3), (R.d_rm>0?"+":"")+f(R.d_rm,1)+" days; "+f(R.greater,3)]],
  [3100, 3130, 3130],
  "Table 1. Out-of-sample comparison against the original full protocol, from the single primary analysis that supplies the observed-cohort point estimates the paper's claims rest on, listed in Supplementary Text S1.6; the nested bootstrap supplies uncertainty only. The eligible-count row is monotone by construction and is reported for completeness rather than as a test. Rotterdam estimates are descriptive: that cohort shows a severe lack of empirical overlap inside the full protocol.")); }

/* ---------- 2 ---------- */
add(H2("Repeated splits are not independent replicates"));
if (NS.colon && PR.colon) {
  const C = NS.colon, R = NS.rott, PC = PR.colon, PRR = PR.rott;
  const ci  = z => "[" + f(z.ci80[0],2) + " to " + f(z.ci80[1],2) + "]";
  const ci1 = z => "[" + f(z.ci[0],1) + " to " + f(z.ci[1],1) + "]";
  add(P("Resampling the cohort around the whole split procedure separates algorithmic stability from population uncertainty. Because a bootstrap resample contains duplicate copies of a patient, the inner split is taken over unique patient identifiers so that no patient contributes to both halves; an earlier version of this analysis split the resampled rows directly, which allowed copies of one patient to fall on opposite sides, and the numbers below are the corrected ones. Correcting it narrowed rather than widened the ranges."));
  add(P("With " + C.B + " outer resamples and " + C.R + " inner splits each, the standard deviation of the colon probability of a lower out-of-sample hazard ratio across resamples is " + f(C.lower.sd_total,3) + ". Of that, " + f(C.lower.sd_mc,3) + " is the inner Monte Carlo component, estimated from the sample variance of the per-split values within each resample, and " + f(C.lower.sd_outer,3) + " is the outer-sampling component obtained by subtracting variances. The naive across-split standard error implied by " + f0(PC.R) + " splits of the observed cohort is " + f(PC.se_lower,4) + ", so the outer-sampling component alone is " + f(C.lower.sd_outer/PC.se_lower,1) + " times it."));
  add(P("The central 80% of the outer distribution runs " + ci(C.lower) + " in colon and " + ci(R.lower) + " in Rotterdam; the 2.5 to 97.5 per cent span is " + ci1(C.lower) + " and " + ci1(R.lower) + ", printed to one decimal because at " + C.B + " resamples its endpoints rest on about the third replicate from each end. Either way the statistic moves over more than half the unit interval when the patients are resampled: the point estimates of " + f(PC.lower,3) + " and " + f(PRR.lower,3) + " are properties of these datasets rather than estimates of how often the procedure would succeed elsewhere, and an in-sample evaluation on a single cohort has access to neither source of variability. The eligibility probability moves much less, " + ci(C.more) + " in colon, as one expects of a quantity that is monotone by construction."));
}

/* ---------- 3 ---------- */
add(H2("Cohort size alone did not determine reliability across the mechanisms examined"));
if (TS && POP) {
  const first = TS.rows[0], last = TS.rows[TS.rows.length-1];
  const mid = TS.rows.filter(r => r.n === 5167)[0] || TS.rows[TS.rows.length-2];
  add(P("With the scoring set fixed at 100 000 patients, the eligibility promise was kept at every fitting size (Figure 2), between " + f(Math.min.apply(null, TS.rows.map(r=>r.p_more)),3) + " and " + f(Math.max.apply(null, TS.rows.map(r=>r.p_more)),3) + " across the ladder, though not monotonically: the smallest value falls at " + f0((TS.rows.filter(r=>r.p_more===Math.min.apply(null,TS.rows.map(q=>q.p_more)))[0]||{}).n) + " fitting patients rather than at the smallest size. The effect-estimate promise rose from " + f(first.p_lower,3) + " to " + f(last.p_lower,3) + " over the same range, reaching " + f(mid.p_lower,3) + " at " + f0(mid.n) + ". At the population level the rule was correct: it identified the diluting criterion, retaining " + f0(POP.n_kept) + " of " + f0(POP.p) + " criteria, and its protocol lowered the hazard ratio from " + f(POP.hr_full,4) + " to " + f(POP.hr_rule,4) + " while raising the eligible count from " + f0(POP.n_full) + " to " + f0(POP.n_rule) + ". The observed colon value sits close to this curve, but the range around it, from the previous section, spans more than half the unit interval, so the agreement has almost no power to discriminate and we draw no confirmation from it."));
}
if (has(FA.n_med)) {
  const FX = NUM.fixedn ? NUM.fixedn.cells : null;
  add(P("That curve belongs to one generating mechanism, and across the resolution-IV factorial the threshold does not transfer: in " + FA.n_right + " of 16 scenarios the crossing lay above the largest fitting size evaluated and in one below the smallest. We therefore report the success probability at fixed fitting sizes, which every scenario supplies; the per-scenario thresholds and their censoring are in Supplementary Table S2."));
  if (FX) {
    add(table(
      ["Fitting patients", "Effect modification concentrated: median [min, max]", "Spread over five criteria: median [min, max]"],
      [["600",   f(FX.n600.conc_med,2)   + " [" + f(FX.n600.conc_lo,2)   + ", " + f(FX.n600.conc_hi,2)   + "]", f(FX.n600.diff_med,2)   + " [" + f(FX.n600.diff_lo,2)   + ", " + f(FX.n600.diff_hi,2)   + "]"],
       ["2 000", f(FX.n2000.conc_med,2)  + " [" + f(FX.n2000.conc_lo,2)  + ", " + f(FX.n2000.conc_hi,2)  + "]", f(FX.n2000.diff_med,2)  + " [" + f(FX.n2000.diff_lo,2)  + ", " + f(FX.n2000.diff_hi,2)  + "]"],
       ["6 000", f(FX.n6000.conc_med,2)  + " [" + f(FX.n6000.conc_lo,2)  + ", " + f(FX.n6000.conc_hi,2)  + "]", f(FX.n6000.diff_med,2)  + " [" + f(FX.n6000.diff_lo,2)  + ", " + f(FX.n6000.diff_hi,2)  + "]"],
       ["18 000",f(FX.n18000.conc_med,2) + " [" + f(FX.n18000.conc_lo,2) + ", " + f(FX.n18000.conc_hi,2) + "]", f(FX.n18000.diff_med,2) + " [" + f(FX.n18000.diff_lo,2) + ", " + f(FX.n18000.diff_hi,2) + "]"]],
      [2600, 3400, 3360],
      "Table 2. Probability of a lower out-of-sample hazard ratio at fixed fitting-cohort size, across all sixteen factorial scenarios, eight per column. Monte Carlo standard error is at most " + ((NUM.fixedn||{}).mc_se_max !== undefined ? f(NUM.fixedn.mc_se_max,3) : "\u2014") + ". No threshold is estimated, so nothing is censored and no scenario is excluded."));
  }
  add(P("The spread across scenarios at any fixed size is large — the standard deviation of the success probability is " + f(NUM.fixedn.cells.n2000.sd_all,2) + " at 2000 fitting patients and " + f(NUM.fixedn.cells.n6000.sd_all,2) + " at 6000, on a scale bounded by one — and an interval-censored regression cannot resolve it, because the arrangement factor completely separates the design (Table S2b)."));
  add(P("We withdraw a claim made in the first revision, that the threshold is more variable in events and effective sample size than in patients: that was computed over the six scenarios in which a threshold was observable, a selected subset. Binning all sixteen by each currency instead, the three summaries are similar — " + f(NUM.fixedn.spread_patients,2) + ", " + f(NUM.fixedn.spread_events,2) + " and " + f(NUM.fixedn.spread_ess,2) + " — but similar descriptive summaries over sixteen scenarios cannot establish that the currencies are equivalent. The observed differences are " + f((NUM.fixedn.currency_loso||{}).events_minus_patients,3) + " for events minus patients and " + f((NUM.fixedn.currency_loso||{}).ess_minus_patients,3) + " for effective sample size minus patients, and leaving each scenario out in turn moves the first over " + f(((NUM.fixedn.currency_loso||{}).range_events||[])[0],3) + " to " + f(((NUM.fixedn.currency_loso||{}).range_events||[])[1],3) + " and the second over " + f(((NUM.fixedn.currency_loso||{}).range_ess||[])[0],3) + " to " + f(((NUM.fixedn.currency_loso||{}).range_ess||[])[1],3) + ", so the similarity is not the work of one or two scenarios. What this supports is a statement about these three descriptive summaries under this binning, not that the currencies are interchangeable (Supplementary Table S2c)."));
  add(P("The 0.80 reliability target is a convention borrowed from power calculations and nothing depends on it: at 0.70 and 0.90 the median located threshold is " + (TG ? f0(TG.t70.med) + " and " + f0(TG.t90.med) : "\u2014") + " fitting patients and the separation between the two arrangements is present at all three targets, though not identical across them (" + (TG ? f0(TG.t70.diff_located) : "\u2014") + " diffuse scenario reaches 0.70 and none reaches 0.80 or 0.90) (Table S2b)."));
}
add(P("The factorial contrasted modification concentrated in one criterion with modification spread over five, and describing the result as being about concentration turned out to be wrong. In that design the per-criterion coefficient was simply divided, so the diffuse arm also had a weaker DILUTING coefficient — " + f(FA.dil_diff,3) + " against " + f(FA.dil_conc,2) + " — and dilution is what the rule must detect, since relaxation can lower the hazard ratio only by dropping a criterion that selects patients who benefit less. Concentration and detection strength were confounded."));
if (CN && CN.A_dil1_enr1) {
  const g = k => CN[k].rows.map(r => f(r.p_lower,2)).join(" / ");
  add(P("A separate sweep separates them, holding the total modification fixed at 0.85 and varying only its arrangement. With the diluting coefficient fixed at 0.30, success at 600, 2000, 6000 and 18 000 fitting patients runs " + g("A_dil1_enr1") + " with the enrichment in one criterion, " + g("B_dil1_enr2") + " split over two and " + g("C_dil1_enr4") + " split over four. Spreading the enrichment helps at 6000 patients, by " + (CD ? f(CD.B_dil1_enr2.rows[2].diff,2) + " (95% Monte Carlo interval " + f(CD.B_dil1_enr2.rows[2].lo,2) + " to " + f(CD.B_dil1_enr2.rows[2].hi,2) + ") and " + f(CD.C_dil1_enr4.rows[2].diff,2) + " (" + f(CD.C_dil1_enr4.rows[2].lo,2) + " to " + f(CD.C_dil1_enr4.rows[2].hi,2) + ")" : "\u2014") + " — so it is not, as we first wrote, without effect. At the other three sizes the intervals include zero, and at 600 the point estimates run the other way (" + (CD ? f(CD.B_dil1_enr2.rows[0].diff,2) + " and " + f(CD.C_dil1_enr4.rows[0].diff,2) : "\u2014") + "), so the direction is established at one size only. Splitting the DILUTION goes the other way at every size (Supplementary Table S4), with differences from the first arrangement of " + (CD ? CD.D_dil2_enr1.rows.map(r => f(r.diff,2)).join(", ") : "\u2014") + ", every interval excluding zero."));
  add(P("Two cautions belong with that comparison. The arrangements hold the full-protocol hazard ratio exactly equal, since under the full protocol the treated bonus is the sum of the coefficients, which the design fixes; they do not hold the ATTAINABLE improvement equal. On a separate " + (CT ? fk(CT.n_eval) : "\u2014") + "-patient evaluation set, larger than the " + (SZ ? fk(SZ.arrangement_scoring) : "\u2014") + "-patient scoring set used for the success probabilities above, the best subset reaches " + (CT ? f(CT.hr_oracle_conc,4) : "\u2014") + " against a full-protocol " + (CT ? f(CT.hr_full,4) : "\u2014") + " in the three arrangements that concentrate the dilution, a gap of " + (CT ? f(CT.gap_conc,4) : "\u2014") + ", and " + (CT ? f(CT.hr_oracle_split,4) : "\u2014") + " in the one that splits it, a gap of " + (CT ? f(CT.gap_split,4) : "\u2014") + ". Splitting the dilution halves the prize as well as the coefficient, and the two cannot be separated, because the attainable gap is itself determined by how the dilution is distributed. Arrangements that concentrate it present both a larger coefficient and a larger prize and are correspondingly easier; the sweep does not isolate the coefficient."));
add(P("The second is that a raw coefficient is unlikely to be the general governing quantity at all: the same coefficient on a criterion retaining 90% of a cohort and on one retaining 30% carries different information, and detectability must also depend on prevalence, correlation among criteria, retained events, censoring and weight variability. A criterion-specific signal-to-noise ratio — the population Shapley value of the diluting criterion over the standard deviation of its estimate at the fitting size — is the more plausible candidate. We report what we were able to test of it in the Discussion and in Supplementary Table S4b, where the ratio is varied across arrangements and fitting sizes together; the short version is that it orders the cells better than the fitting size does, that the comparison which would carry information is between arrangements at a fixed size, and that four arrangements cannot establish a governing quantity. We offer it as the direction a general answer would take rather than as one."));
add(P("What survives is the consequence for planning. Whatever the right summary turns out to be, it is a property of the effect modification present, which is the quantity the analysis exists to estimate. Positivity, logical implication and interaction leverage can all be computed before any outcome is examined; the strength of the diluting signal cannot. So a data requirement cannot be read off cohort size, event count or effective sample size alone. It can be stated conditionally, on an assumed minimum diluting signal, in the same way that a conventional power calculation is stated on an assumed minimum clinically relevant effect. We do not claim to supply that conditional statement here: Table 2 reports success at fixed fitting sizes across arrangements, which is a sensitivity analysis and not a family of design curves, and we have not established the quantity such curves would have to be indexed by. What the simulations do rule out is a portable cohort-size-only threshold of the kind we ourselves offered in an earlier version: across sixteen factorial scenarios and one arrangement sweep, no single fitting size delivers the effect-estimate promise everywhere. We are careful about the scope of that. Sixteen scenarios and one sweep establish heterogeneity among the mechanisms we examined; they are not an impossibility proof, and we make no claim about generating mechanisms outside them."));
}
add(FIG("fig_trainsize.png", 618, 260,
  "Figure 2. Left: probability that each promise is kept by fitting-cohort size in the single-mechanism simulation, scoring set fixed at 100 000, with Monte Carlo intervals. The observed cohort points carry the patient-resampled ranges from the nested resampling, which span more than half the unit interval; their apparent agreement with the simulated curve has little discriminating power and we draw no confirmation from it. Right: the probability that the rule recovers the exactly correct criterion set."));

/* ---------- 4 ---------- */
add(H2("Below the threshold the selected protocol can be slightly worse than the original"));
if (RC && RC.length) {
  const A0 = RC[0], AZ = RC[RC.length-1];
  add(P("Exact recovery of the criterion set rose from " + f(A0.exact) + " at " + f0(A0.n) + " fitting patients to " + f(AZ.exact) + " at " + f0(AZ.n) + ", but that is an unnecessarily strict standard when several protocols perform almost identically. The softer measures move monotonically: Hamming distance fell from " + f(A0.hamming,2) + " criteria to " + f(AZ.hamming,2) + ", Jaccard similarity rose from " + f(A0.jaccard) + " to " + f(AZ.jaccard) + ", false retention fell from " + f(A0.false_retain) + " to " + f(AZ.false_retain) + " and false relaxation from " + f(A0.false_relax) + " to " + f(AZ.false_relax) + ". Over-relaxation is the error that persists."));
  add(P("Regret needs defining before it is read. Ours is on the log hazard-ratio scale: the difference between the population log hazard ratio under the selected protocol and under the subset that minimises it, both evaluated on the fixed 100 000-patient scoring set. It is a single-objective measure that ignores eligible count, safety and every other clinical consideration, and because different protocols target different populations a larger regret means further from the lowest attainable hazard ratio rather than worse for a patient. The full protocol has a regret of its own, which is what makes the two comparable. In this generating model the rule's population limit set coincides with the minimising subset, so its asymptotic regret is zero; the Shapley-sign rule carries no general guarantee of reaching the global optimum when interactions are present and we claim this for this scenario only. Regret falls from " + f(A0.regret,4) + " at " + f0(A0.n) + " fitting patients to " + f(AZ.regret,4) + " at " + f0(AZ.n) + ". The full protocol's regret is " + f(NUM.recovery_full_regret,4) + ". The two cross near a thousand fitting patients: below that, the protocol the rule produces sits slightly further from the lowest attainable hazard ratio than the full protocol does. Read narrowly — as a statement about this one generating model and this one objective — it means the expected outcome for a small cohort is not a smaller improvement but a marginally worse position on that objective, applied to a larger group of patients. It is not a claim that the resulting protocol is clinically worse, which would need the other objectives weighed alongside it."));
}

/* ---------- 5 ---------- */
add(H2("Confounding moves the threshold, and unmeasured confounding hides itself"));
if (CF) add(P("Correctly adjusted confounding lowered the curve throughout (Figure 3): at " + f0(5167) + " fitting patients the probability fell from " + f(CF.rand_5167,3) + " to " + f(CF.adj_5167,3) + ". How much extra sample that costs depends on the reliability asked for. The randomised arm reaches " + f(CF.rand_3000,2) + " at 3000 fitting patients and the adjusted arm reaches about the same value at 5167, a factor of about " + f(5167/3000,1) + "; to reach the " + f(CF.rand_5167,2) + " the randomised arm has at 5167, the adjusted arm needs more than the largest size we simulated, where it stands at " + f((CF.rows.filter(r=>r.n===20000 && /adjust/.test(r.arm))[0]||{}).p,2) + "."));
add(P("The third arm is the more consequential result and requires care with language. Because all three arms share one outcome model, the true causal contrast is identical in all three by construction; what differs is the population limit of the weighted estimator under each adjustment. Omitting one confounder returned the reliability curve to randomised levels — " + f(CF.unmeas_5167,3) + " at 5167 against " + f(CF.rand_5167,3) + " — which might easily be read as reassurance. The estimator's population limit for the gap between protocols rose to " + (CL ? f(CL.gap_unmeas,4) : "\u2014") + " from " + (CL ? f(CL.gap_rand,4) : "\u2014") + ", " + (CL ? pct(CL.inflation,1) : "\u2014") + " larger, with the whole hazard ratio displaced from " + (CL ? f(CL.hr_rand,3) : "\u2014") + " to " + (CL ? f(CL.hr_unmeas,3) : "\u2014") + ". Meanwhile the probability of recovering the correct criterion set fell from " + f(CF.rec_rand_5167,3) + " to " + f(CF.rec_unmeas_5167,3) + ". The procedure appeared equally reliable while its selection was in fact less accurate, because the quantity it was detecting had itself been inflated by bias."));
add(P([R("Observing that the method performs well on real-world data therefore does not by itself indicate that adjustment was adequate. A useful sensitivity analysis compares the estimated gap under richer and poorer adjustment and expects it to shrink as adjustment improves — but that expectation holds only when the added variables are plausibly confounders. Adding an instrument can amplify residual bias"), CITE("myers2011"), R(" and adding a collider or mediator can create it, so the direction of travel is a warning worth heeding among substantively defensible adjustment sets, not a test that settles the question.")]));
add(FIG("fig_confound.png", 618, 250,
  "Figure 3. Three treatment-assignment mechanisms sharing one outcome model, with Monte Carlo intervals. Left: correctly adjusted confounding lowers the curve; omitting one confounder returns it to randomised levels. Centre: the population limit of the estimated gap is " + (CL ? pct(CL.inflation,1) : "\u2014") + " larger under unmeasured confounding — the true causal gap is identical in all three arms. Right: correct selection meanwhile falls by more than half."));

/* ---------- 6 ---------- */
add(H2("Why the hazard ratio is the fragile leg"));
add(P("Under proportional hazards (Figure 4), with the conditional hazard ratio fixed at " + (NCol ? f(NCol.cond_hr,1) : "\u2014") + " in every stratum, a criterion defined on the prognostic covariate changes no individual's relative benefit, yet its Shapley value on the log hazard ratio grew with prognostic strength, though not monotonically at the top of the range, from " + (NCol ? f(NCol.phi_hr_zero,4) : "\u2014") + " with no heterogeneity to " + (NCol ? f(NCol.phi_hr_max,3) : "\u2014") + " at the strongest setting, while the marginal hazard ratio drifted from " + (NCol ? f(NCol.hr_all_zero,3) : "\u2014") + " to " + (NCol ? f(NCol.hr_all_max,3) : "\u2014") + ". The value at zero heterogeneity is the falsification point and the artefact vanished there. The restricted mean difference also moved, and in the opposite direction: it fell from " + (NCol ? f(NCol.rm_all_weak,3) : "\u2014") + " in the whole cohort to " + (NCol ? f(NCol.rm_sub_weak,3) : "\u2014") + " in the selected group at the weakest setting and from " + (NCol ? f(NCol.rm_all_strong,3) : "\u2014") + " to " + (NCol ? f(NCol.rm_sub_strong,3) : "\u2014") + " at the strongest, so its Shapley value is negative wherever there is heterogeneity to collapse over (" + (NCol ? f(NCol.rows[1].phi_rmstD,3) : "\u2014") + " at the weakest non-zero setting, " + (NCol ? f(NCol.rows[NCol.rows.length-1].phi_rmstD,3) : "\u2014") + " at the strongest) and is " + (NCol ? f(NCol.rows[0].phi_rmstD,4) : "\u2014") + " at zero heterogeneity, where there is nothing to collapse. That is the expected direction and it corresponds to a real fact — a lower-risk group has less absolute survival to gain, because more of it survives the horizon regardless of treatment. The hazard-ratio movement corresponds to no fact of either kind, which is the concrete reason a lower hazard ratio must not be read as a better estimate."));
add(FIG("fig_sim.png", 618, 250,
  "Figure 4. Non-collapsibility as a dose–response. The conditional hazard ratio is 0.6 in every stratum at every point on these curves, so the criterion changes no individual's relative benefit; the Shapley value on the log hazard ratio nonetheless grows with prognostic strength and vanishes where there is no heterogeneity to collapse over."));

/* ---------- 7 ---------- */
add(H2("Pairwise interactions here are attenuated, not absent"));
add(P([R("The plate plot, adapted here from network meta-analysis"), CITE("ren2026"), R(", displays both estimands in one panel (Figure 5). Leverage is signed: positive correlation raises the joint eligibility rate above the product of the marginals, and once it rises far enough the factor can become negative, meaning the contrast flips sign rather than vanishing. Magnitude therefore measures attenuation. In colon the median magnitude was " + (PL ? f(PL.colon.lev_med,3) : "\u2014") + " and the maximum " + (PL ? f(PL.colon.lev_max,3) : "\u2014") + ", with " + (PL ? f0(PL.colon.n_above_10) : "\u2014") + " pairs of " + (PL ? f0(PL.colon.npair) : "\u2014") + " above 0.10 and " + (PL ? f0(PL.colon.n_below_05) : "\u2014") + " of " + (PL ? f0(PL.colon.npair) : "\u2014") + " below 0.05; in Rotterdam the median was " + (PL ? f(PL.rott.lev_med,3) : "\u2014") + ", " + (PL ? f0(PL.rott.n_above_10) : "\u2014") + " pairs exceeded 0.10 and " + (PL ? f0(PL.rott.n_below_05) : "\u2014") + " of " + (PL ? f0(PL.rott.npair) : "\u2014") + " fell below 0.05.")]));
add(P("The consequence is a power statement, not a structural one. A global permutation test on the colon interaction matrix returned p = " + (PL ? f(PL.colon.perm_hr,2) : "\u2014") + " on the hazard ratio and " + (PL ? f(PL.colon.perm_rm,2) : "\u2014") + " on the restricted mean, and no main effect survived multiplicity adjustment on either estimand. Because the contrast in these cells is attenuated by a factor of order " + (PL ? f(PL.colon.lev_med,2) : "\u2014") + " to " + (PL ? f(PL.colon.lev_max,2) : "\u2014") + ", those nulls carry little information about effect modification: they bound what could have been detected at this attenuation and sample size and nothing more. The one class of cells about which a null is definitive is the logically implied pair, where the contrast is exactly zero and which the implication detector removes before testing."));
if (NUM.coverage) {
  const C = NUM.coverage;
  add(P("Against known truth over " + C.reps + " replicates at n = " + (SZ ? f0(SZ.coverage_n) : "\u2014") + ", the bias-corrected and accelerated interval attained " + f(C.bca_null,3) + " coverage (Monte Carlo SE " + f(C.se_null,3) + ") for a null interaction, against a nominal 0.95, while the percentile interval over-covered at " + f(C.pct_null,3) + ". Since most cells of a real interaction matrix are null and their false-positive behaviour determines what gets reported, that is the comparison that matters. For a genuinely non-null interaction both under-covered, at " + f(C.bca_planted,3) + " and " + f(C.pct_planted,3) + " (Monte Carlo SE " + f(C.se_planted,3) + "). We recommend at least 1000 bootstrap replicates and 50 jackknife groups, and treating intervals for large interactions as mildly anticonservative."));
} else {
  add(P("[Coverage results pending.]"));
}
if (PR.colon && PR.colon.same_set !== undefined) {
  add(P("Because the hazard-ratio point estimates favour the rule in both cohorts while the two cohorts point opposite ways on absolute benefit, we asked what happens if the rule selects on the restricted mean difference instead — retaining every criterion whose RMST Shapley value is positive. The two rules are close to different procedures: they agree on the criterion set in a proportion " + f(PR.colon.same_set,3) + " of colon splits and " + f(PR.rott.same_set,3) + " of Rotterdam splits, and in colon the RMST rule admits " + f0(PR.colon.n_rmrule) + " patients against the hazard-ratio rule\'s " + f0(PR.colon.n_rule) + " and the full protocol\'s " + f0(PR.colon.n_full) + "."));
  add(P("Out of sample the RMST-selected variant exceeded the full protocol\'s restricted mean difference in " + pct(PR.colon.rmrule_greater) + " of colon splits against " + pct(PR.colon.greater) + " for the published rule, and reached a lower hazard ratio in " + pct(PR.colon.rmrule_lower) + " against " + pct(PR.colon.lower) + "." + (NS.colon && NS.colon.rmrule_greater ? " Carried through the same patient resampling, and stated as marginal figures against the full protocol rather than as a comparison between the rules, these are " + pct(PR.colon.rmrule_greater) + ", central 80% range " + iv2(NS.colon.rmrule_greater) + ", and " + pct(PR.colon.rmrule_lower) + ", " + iv2(NS.colon.rmrule_lower) + "; the agreement rate itself moves over " + iv2(NS.colon.rm_same) + "." : "") + " We therefore do not claim that switching the estimand gains nothing. Nor do we claim the opposite. Both quantities are probabilities of improvement over the full protocol, and they go in different directions: the probability of a greater restricted mean difference is higher for the RMST-selected variant, and the probability of a lower hazard ratio is lower for it. Two figures each formed against the full protocol cannot settle which rule is better, because the rules share patients and splits and their errors are correlated, so we compute the difference between them inside each split and carry that paired difference through the same outer bootstrap. The paired advantage of the RMST-selected variant is " + f(PR.colon.d_pair_greater,3) + (NS.colon && NS.colon.d_pair_greater ? ", range " + iv2(NS.colon.d_pair_greater) : "") + " on the restricted mean difference and " + f(PR.colon.d_pair_lower,3) + (NS.colon && NS.colon.d_pair_lower ? ", " + iv2(NS.colon.d_pair_lower) : "") + " on the hazard ratio in colon; in Rotterdam they are " + f(PR.rott.d_pair_greater,3) + (NS.rott && NS.rott.d_pair_greater ? ", " + iv2(NS.rott.d_pair_greater) : "") + " and " + f(PR.rott.d_pair_lower,3) + (NS.rott && NS.rott.d_pair_lower ? ", " + iv2(NS.rott.d_pair_lower) : "") + ". Each paired central range spans about a quarter of a unit and lies on both sides of zero, and the paired ranges are narrower than the two marginal ones, which is the point of pairing. We do not read the ranges as tests and make no claim that either rule is better or that the two are equivalent; we did not prespecify an equivalence margin either. What the data do show is that the two decompositions select almost entirely different criterion sets, so the choice of decomposition estimand is a real design decision even though these cohorts cannot say which choice is better."));

}
if (NUM.horizon) {
  const HZc = NUM.horizon.colon, HZr = NUM.horizon.rott;
  const rng = (T, k) => f(Math.min.apply(null, T.map(r => r[k])),3) + " to " + f(Math.max.apply(null, T.map(r => r[k])),3);
  add(P("The horizon matters for both of these comparisons and was not justified in the previous version. The restricted mean must be taken at a time inside the observed follow-up of both arms of every candidate protocol, or subsets are not compared on the same scale; we used " + f0(NUM.horizon.tau_colon) + " days in colon and " + f0(NUM.horizon.tau_rott) + " days in Rotterdam, at which at least " + pct(NUM.horizon.risk_colon) + " and " + pct(NUM.horizon.risk_rott) + " of patients in either arm are still under observation. Repeating the selection comparison at four horizons per cohort, the two decompositions agree on the criterion set in " + rng(HZc, "same") + " of colon splits and " + rng(HZr, "same") + " of Rotterdam splits, with mean Jaccard similarity of " + rng(HZc, "jac") + " and " + rng(HZr, "jac") + " respectively. The disagreement between the two estimands is therefore a property of the estimands rather than of the particular horizon, but the exact agreement rate is horizon-dependent and should be reported with the horizon attached (Supplementary Table S3d)."));
  add(P("The horizon also changes the sign of the absolute-benefit comparison, which we had not looked for. In colon the mean out-of-sample restricted mean difference is " + f(HZc[0].rmF,1) + " days for the full protocol at the shortest horizon we tried against " + f(HZc[0].rm1,1) + " for the published rule and " + f(HZc[0].rm2,1) + " for the RMST-selected variant, so both relaxed protocols are ahead; at the longest horizon the full protocol leads both (" + f(HZc[3].rmF,1) + " against " + f(HZc[3].rm1,1) + " and " + f(HZc[3].rm2,1) + "). At the horizon used in the main analysis the three are close and the ordering is mixed (" + f(HZc[2].rmF,1) + ", " + f(HZc[2].rm1,1) + " and " + f(HZc[2].rm2,1) + "). A statement that relaxation does or does not improve absolute benefit is therefore not well posed without the horizon, our main-text figures sit in the middle of the range we examined, and the crossing means no single horizon should be read as the answer. We report the horizon everywhere and do not generalise beyond it."));
}

add(FIG("fig2_plate3.png", 440, 522,
  "Figure 5. The criteria plate plot for colon. Upper triangle, pairwise interaction on the log hazard ratio; lower triangle, on the restricted mean difference; diagonal, per-criterion main effects on both estimands, shaded by the fraction of the cohort the criterion excludes. Light hatching marks cells whose leverage is below 0.05 in magnitude — 35 of 36 here — and dense hatching cells that are arithmetic identities. A full-page version is Supplementary Figure S2."));

/* ================= DISCUSSION ================= */
add(H1("Discussion"));
add(P("Data-driven relaxation admits more patients, and it is worth being precise about what that shows. The rule retains a subset of the criteria, so the eligible set can only grow, and the probability reported for this promise measures how often a binding criterion is removed. Scored against all 512 subsets on the held-out half, the rule's choice is almost never on the empirical eligible-count-versus-hazard-ratio frontier. That frontier is itself optimistic, and the size of the correction is not small: when the comparator subsets are chosen on one third of the cohort and the rule is judged against exactly those on a third that has been used for nothing, it is left undominated in " + (TW ? pct(TW.colon.front_conf) + " of colon and " + pct(TW.rott.front_conf) + " of Rotterdam splits, against " + pct(TW.colon.front_sel) + " and " + pct(TW.rott.front_sel) + " judged on the selection third of the same three-way design" : "a substantially larger share of splits") + ". The comparison that isolates selection optimism is the one within this design, since the half-split figures reported earlier also use a larger evaluation set and the two effects cannot be separated across designs. The qualitative finding survives — the rule is beaten in most splits on every reading — but the out-of-sample figure is the second one, and quoting only the first overstates the shortfall. Matched on eligible count, its hazard-ratio advantage moves over about a third of the unit interval when the patients are resampled, and on the absolute-benefit estimand the two cohorts point opposite ways. So the rule is not optimising the trade-off between the two promises, and whether it has any advantage at equal inclusiveness is a question these cohorts cannot settle — neither of which a comparison against the full protocol alone can show."));
add(P("What a wider eligible population is worth clinically is a separate question no outcome model answers, and criteria are not all of a kind: those encoding safety, tolerability, feasibility or pharmacology should be designated non-relaxable before the procedure runs rather than left to a hazard ratio to adjudicate."));
add(P("Whether the effect-estimate improvement is attainable depends on something that cannot be checked before the analysis, but that does not make planning impossible. Across the factorial the success probability varies by between " + f((NUM.fixedn||{}).sd_min,2) + " and " + f((NUM.fixedn||{}).sd_max,2) + " in standard deviation across the fixed sample sizes, averaging " + (FX2 !== undefined && FX2 !== null ? f(FX2,2) : "\u2014") + ", and grouping scenarios by events or by effective sample size rather than by patients did not reduce that spread in this design (" + f(NUM.fixedn.spread_patients,2) + ", " + f(NUM.fixedn.spread_events,2) + " and " + f(NUM.fixedn.spread_ess,2) + "), though with " + f0((NUM.fixedn.currency_loso||{}).n_scenarios || 16) + " fixed design points this comparison cannot establish that the three are equivalent, and the most that can be said is that the three chosen descriptive summaries came out numerically similar under this particular binning, robustly to leaving any one scenario out (Supplementary Table S2c) — a claim we made in the previous revision and withdraw here, because it was computed only over the scenarios in which a threshold happened to be observable. We are not able to name the quantity that does govern the requirement. A sentence in the previous revision named the magnitude of the largest diluting effect-modification coefficient, and we withdraw it: our own results do not support it. Arranging the same total modification differently is not neutral — spreading the enrichment raised success by " + (CD ? f(CD.B_dil1_enr2.rows[2].diff,2) + ", Monte Carlo interval [" + f(CD.B_dil1_enr2.rows[2].lo,2) + ", " + f(CD.B_dil1_enr2.rows[2].hi,2) + "]" : "\u2014") + ", at 6000 — and halving the diluting coefficient also roughly halves the attainable gap, so in that design the coefficient and the size of the prize move together and cannot be separated. What can be said is weaker and, we think, still useful: the requirement depends on the strength of the diluting signal relative to the noise at the fitting size, and that strength is what the analysis exists to estimate, so no requirement in patients, events or effective sample size can be given unconditionally. This is the ordinary position of a power calculation, whose sample size also depends on an effect size not yet observed. A criterion-specific signal-to-noise ratio — the population Shapley value of the most diluting criterion over the standard deviation of its estimate at the fitting size — is the candidate we tried, and we report it with the caveat that matters. Pooled over " + (NUM.snr_curve ? NUM.snr_curve.cells.length : "—") + " simulated cells it orders success better than the fitting size does (Spearman " + (NUM.snr_curve ? f(NUM.snr_curve.sp_snr,2) + " against " + f(NUM.snr_curve.sp_n,2) : "—") + "), but that pooled figure is not evidence for the ratio: within an arrangement the ratio rises with the fitting size by construction, so the within-arrangement association (" + (NUM.snr_curve && isFinite(NUM.snr_curve.sp_within) ? f(NUM.snr_curve.sp_within,2) : "—") + " after adjusting for arrangement) says little more than that success rises with sample size. The comparison that would carry information is between arrangements at a fixed size, where the association is weaker (" + (NUM.snr_curve && isFinite(NUM.snr_curve.sp_between) ? f(NUM.snr_curve.sp_between,2) : "—") + " at the largest size tried) and rests on four arrangements, one of which shows no variation in success at all. We therefore do not claim the ratio as the governing quantity, only as the direction we would pursue next (Supplementary Table S4b). What we can rule out is a portable cohort-size-only threshold of the kind we ourselves offered in a previous version — within the mechanisms examined here, and as a statement about those mechanisms rather than a general impossibility result. Table 2 should accordingly be read as a sensitivity analysis across arrangements at fixed fitting sizes, not as a design curve to read a requirement from. The result also separates cleanly what an investigator can check beforehand — empirical overlap inside the full protocol, logical implication, interaction leverage — from what they cannot."));
add(P("For small cohorts the regret comparison adds a further caution: the protocol the rule produces sits slightly further from the lowest attainable hazard ratio than the original does. That is a single-objective statement about one generating model, not a claim of clinical inferiority."));
add(P("The unmeasured-confounding arm is the finding we would most want carried forward. Residual confounding inflated the quantity being detected and thereby made the procedure appear more reliable while its selection became less accurate, which runs against the usual intuition that a clear signal is reassuring. The recommendation that follows — compare adjustment sets and expect the gap to shrink — is a heuristic with real exceptions and should be used among substantively defensible sets rather than as a test."));
add(P("Rotterdam is a cautionary example of a different kind. Chemotherapy there went almost exclusively to younger, premenopausal women, so within the restrictive full protocol there is very little overlap and no specification we tried restores balance — exactly the check we argue should come first. Restriction does not create it; overlap declined only slightly with the number of criteria applied in both cohorts."));
add(P("Three by-products may be useful independently. Interaction leverage is a closed-form quantity requiring no outcome data that says how far a pair's interaction is attenuated by the eligibility rates alone; we have corrected the claim made for it — low leverage is attenuation rather than structural invisibility — and derived the coefficient for three interaction forms, all multiples of (1−q)(1−q) under independence, exact for the both-criteria structure and approximate for the other two. The implication detector removes cells that are arithmetic identities, the only cells genuinely uninformative regardless of sample size. And carrying both estimands makes visible where a criterion's relative and absolute effects disagree, a substantive question that should be settled on clinical grounds rather than by a software default."));
add(P("Several limitations bound these conclusions. The criteria were constructed from available covariates rather than transcribed from protocols, so this demonstrates a general property rather than evaluating any trial, and neither cohort is the database used in the original work. The factorial is resolution IV, so two-factor interactions are aliased in pairs; the criterion-count factor spans only 6 against 8, because exhaustive enumeration costs 2^p evaluations per replicate; and propensity overlap, weight variability, missingness and model misspecification are represented crudely or not at all. All simulated quantities depend on their generating models. The absolute-benefit column depends on an analyst choice of horizon, and in colon its sign flips across a plausible range of that choice. The leverage expressions are derived for three specific interaction forms."));

add(H1("Conclusions"));
add(P("Relaxing trial eligibility criteria from data widens the eligible population, but that widening follows largely from the structure of the rule, and scored against the subsets attainable on held-out patients the rule's choice is rarely on the eligible-count-versus-hazard-ratio frontier, on both the empirical reading and the stricter one in which the comparator subsets are fixed on separate data first, though the two differ by a factor of several and only the second is an out-of-sample statement. Whether it contributes anything at matched inclusiveness is not resolved by cohorts of this size; the point estimates favour it on the hazard ratio in both cohorts while the two cohorts point opposite ways on absolute benefit, and a variant selecting on absolute benefit instead chooses almost entirely different criteria, and showed no clear or consistent advantage on either estimand; the paired difference between the two rules lies on both sides of zero, and no equivalence margin was prespecified, so we claim neither an advantage nor equivalence. How much data that contribution requires cannot be determined from patients, events or effective sample size alone: across the mechanisms we simulated it depends on the strength of the diluting signal, which is the quantity the analysis exists to discover. We have not identified what does govern it, and we do not claim that no such requirement can exist. It can in principle be stated conditionally on an assumed minimum such signal, as a power calculation is stated on an assumed minimum effect, though we do not supply that conditional statement here. Incomplete confounding adjustment can make the procedure appear more successful while its selection is less accurate, so apparent success on real-world data should not be taken as evidence of adequate adjustment. What an investigator can establish beforehand is narrower than we previously suggested — empirical overlap inside the full protocol, logical implication among criteria, and interaction leverage — and it does not include whether the effect-estimate promise is within reach."));
};
